import chai from "chai";

const { assert } = chai;

// TODO: Ajouter un test pour valider la logique de validation des réponses
// La casse et les charactères spéciaux sont ignorés

it("should work", () => {
  assert.equal(true, true);
});
